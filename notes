#include <iostream>
using namespace std;

bool pair(int arr[], int n) {
  // check if any two elements match
      // ex: arr[5] = {6, 2, 4, 1, 10} ==> false
        // arr2[4] = {6, 3, 4, 3} ==> true
      // outline your solution (english terms)
}
